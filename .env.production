# Production environment — baked into React build at compile time.
# This points to the Render backend service.
# ⚠️ Update this URL if your Render backend service has a different name.
REACT_APP_API_URL=https://smart-edu-backend.onrender.com
